import { dark, ligth } from './colors'
import { padding, margin } from './spaces'
import { fontFamily, fontSize } from './fonts'

export const darkTheme = {
  colors: dark,
  fontSize,
  padding,
  margin,
  fontFamily
}

export const ligthTheme = {
  colors: ligth,
  fontSize,
  padding,
  margin,
  fontFamily
}
